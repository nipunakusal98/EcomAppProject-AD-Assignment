@*Define Model*@

@model IEnumerable<Series>

@*Define ViewData*@

@{
    ViewData["Title"] = "Computer Series.";
}
<h1>Customers</h1>

<div class="row">
    <div class="col-md-offset-md-2">
        <table class="table">
            <thead>
                <tr>
                    <th>@Html.DisplayNameFor(model=>model.SeriesPictureURL)</th>
                    <th>@Html.DisplayNameFor(model=>model.SeriesName)</th>
                    <th>@Html.DisplayNameFor(model=>model.CategoryID)</th>
                    

                    <th>Actions</th>
                </tr>
            </thead>

            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td class="align-middle">
                            <img class="rounded-circle" src="@item.SeriesPictureURL" style="max-width: 150px" />
                        </td>
                        <td class="align-middle">
                            @Html.DisplayFor(modelItem => item.SeriesName)
                        </td>
                        <td class="align-middle">
                            @Html.DisplayFor(modelItem => item.CategoryID)
                        </td>
                        

                        <td class="align-middle">
                            <a class="btn btn-outline-primary"><i class="bi bi-pencil-square"></i>Edit</a>
                            <a class="btn btn-outline-info"><i class="bi bi-eye"></i>Details</a>
                            <a class="btn btn-danger"><i class="bi bi-trash"></i>Delete</a>
                        </td>
                    </tr>
                }
            </tbody>

        </table>
    </div>
</div>
