@*Define Model*@

@model IEnumerable<VGA>

@*Define ViewData*@

@{
    ViewData["Title"] = "List Of VGA Cards.";
}
<h1>Welcome</h1>

<div class="row">
    <div class="col-md-offset-md-2">
        <table class="table">
            <thead>
                <tr>
                    <th>@Html.DisplayNameFor(model=>model.VGAPictureURL)</th>
                    <th>@Html.DisplayNameFor(model=>model.VGADescription)</th>
                    <th>@Html.DisplayNameFor(model=>model.VGAPrice)</th>
                    <th>Actions</th>
                </tr>
            </thead>

            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td class="align-middle">
                            <img class="rounded-circle" src="@item.VGAPictureURL" style="max-width: 150px" />
                        </td>
                        <td class="align-middle">
                            @Html.DisplayFor(modelItem => item.VGADescription)
                        </td>
                        <td class="align-middle">
                            @Html.DisplayFor(modelItem => item.VGAPrice)
                        </td>
                        <td class="align-middle">
                            <a class="btn btn-outline-primary"><i class="bi bi-pencil-square"></i>Edit</a>
                            <a class="btn btn-outline-info"><i class="bi bi-eye"></i>Details</a>
                            <a class="btn btn-danger"><i class="bi bi-trash"></i>Delete</a>
                        </td>
                    </tr>
                }
            </tbody>

        </table>
    </div>
</div>